#!/usr/bin/env node
import { Dt as getConfig, yt as exportGtfs } from "../src-SQlgbfyz.js";
import { a as parseFlags, i as SQLITE_PATH_FLAG, n as CONFIG_PATH_FLAG, r as LOG_LEVEL_FLAG, t as handleFatalError } from "../fatal-D6b0btzc.js";

//#region src/bin/gtfs-export.ts
const setupExport = async () => {
	const values = parseFlags("gtfs-export", "Export GTFS from a SQLite database back into csv files.", [
		CONFIG_PATH_FLAG,
		SQLITE_PATH_FLAG,
		LOG_LEVEL_FLAG
	]);
	const config = await getConfig(values);
	await exportGtfs(config);
	process.exit();
};
setupExport().catch(handleFatalError);

//#endregion
export {  };
//# sourceMappingURL=gtfs-export.js.map