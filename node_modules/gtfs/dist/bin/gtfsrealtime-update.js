#!/usr/bin/env node
import { Dt as getConfig, St as updateGtfsRealtime } from "../src-SQlgbfyz.js";
import { a as parseFlags, n as CONFIG_PATH_FLAG, r as LOG_LEVEL_FLAG, t as handleFatalError } from "../fatal-D6b0btzc.js";

//#region src/bin/gtfsrealtime-update.ts
const setupUpdate = async () => {
	const values = parseFlags("gtfsrealtime-update", "Refresh GTFS-Realtime data in a SQLite database.", [CONFIG_PATH_FLAG, LOG_LEVEL_FLAG]);
	const config = await getConfig(values);
	await updateGtfsRealtime(config);
	process.exit();
};
setupUpdate().catch(handleFatalError);

//#endregion
export {  };
//# sourceMappingURL=gtfsrealtime-update.js.map